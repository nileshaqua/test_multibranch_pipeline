pipeline{
         agent
         stages{
         stage("Build"){
              steps{ 'echo "This is build stage"'
                      sh docker version
                   }
                  }
               }
           }
